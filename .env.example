# Optional. Leave unset to use the built-in local fallback generator.
OPENAI_API_KEY=

# Optional. Defaults to gpt-5-mini when OPENAI_API_KEY is set.
OPENAI_MODEL=gpt-5-mini

# Optional. Defaults to http://localhost:3000 for local development.
NEXT_PUBLIC_APP_URL=http://localhost:3000
